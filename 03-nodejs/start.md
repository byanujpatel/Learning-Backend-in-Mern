## Notes on Creating a Server in Express

- Json is lightweight and structured and represented as String
- JSON.stringify -> Object to JSON String
- JSON.parse -> JSON String to Object
- Collective of Endpoints => API
- Individual endpoint are program we have written => Waiter
- Express se API / Menu banana is easiest 
- Waiter hamara system me rahta hai address to hota hi hoga for Reach out
- Methods -> GET,POST,PATCH,DELETE

- Written basic instance of express and store in app
- Created 3 endpoint for three menu home ,paneer,idli endpoint
- first route then function of req,res
- Listen on 3000 on port 
- Also tried to showcase object in Idli Endpoint


## MongoDB - NO SQL Database Notes Now

- Installed mongodb using brew method
- using brew start and brew list services i can see whether database server is running or not
- Created a DB in mongodb and created a collection named User and created a document 
- also using mongosh cmd see db in terminal as well
- show databases,show collections , use db
- used postman using backend server
- Node/Express Server JS Language -> Driver Convert Translator/Checking/Toll (JS -> MongoDB Query)  -> Database Mongo Server in Database Language